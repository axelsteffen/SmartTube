---
description: Update fork-docs/CHANGELOG.md when making fork-specific changes; document milestone progress
alwaysApply: true
---

# Changelog_FORK maintenance

When you add, change, or remove fork-specific code (anything that differs from the original [SmartTube](https://github.com/yuliskov/SmartTube)), update:

1. **[fork-docs/CHANGELOG.md](fork-docs/CHANGELOG.md)** – add to the relevant subproject section (`common`, `smarttubetv`, `MediaServiceCore` summary)
2. **[MediaServiceCore/CHANGELOG_FORK.md](MediaServiceCore/CHANGELOG_FORK.md)** – when changing files in the MediaServiceCore submodule

Include: file path, brief description of the change, and any new APIs or settings.

---

# Milestone progress in the changelog

When implementing a milestone:

1. **Every completed change** is recorded in `fork-docs/CHANGELOG.md` under `[Unreleased]` in a timely manner.
2. **Orientation:** The changelog reflects the current state of the milestone – you can always see which steps are done.
3. **Before the next step:** Check the changelog and the milestone doc in `fork-docs/milestones/` to know the current state and continue seamlessly.
